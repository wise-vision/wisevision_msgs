# AddSubscriberResponse.msg

# Connection status (true if the sensor or component is connected, false otherwise).
# Example: "true"
bool connection     

# Frequency of sensor updates or message processing in Hertz (Hz).
# Example: "10"
uint8 frequency       
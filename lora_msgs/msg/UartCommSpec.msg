# UartCommSpec.msg

# Identifier for the communication port (e.g., UART port number).
# Example: "1"
uint8 port_id

# Baud rate for the communication (speed in bits per second).
# Example: "9600"
uint32 baud